{
    "data_loc": "data",
    "dataloader_loc": "../dataloaders",
    "weights_loc": "../weights",
    "train" : {
        "dataloader": "dataset.pth",
        "weights": "model.checkpoint"
    },
    "eval" : {
        "dataloader": "dataset.pth",
        "weights": "model.checkpoint"
    }
}
